<div>
  <h1><%="Welcome, #{current_user.username}!"%></h1>
  <h2>Game History</h2>
  <% if @rounds.any? %>
  <ul>
    <% @rounds.each do |round| %>
      <li>
        <h3><%= round.cards.first.deck.title %></h3>
        <table>
          <tr>
            <td>Played On:</td>
            <td><%= round.created_at.to_date %></td>
          </tr>
          <tr>
            <td>Total Cards:</td>
            <td><%= round.total_cards %></td>
          </tr>
          <tr>
            <td>Guesses Right on First Try:</td>
            <td><%= round.first_try %></td>
          </tr>
          <tr>
            <td>Total Guesses:</td>
            <td><%= round.total_guesses %></td>
          </tr>
        </table>
      </li>
    <% end %>
  </ul>
  <% else %>
    <p> You haven't played any rounds yet! Click <a href="/gamez">HERE</a> to see our selection of quizzes.</p>
  <% end %>
</div>
